// Define an array
let array = [10, 20, 30, 40, 50];

// Input from the user for the value to check
let userInput = 40;

// Check if the array contains the given value

if (array.includes(userInput)) {
    console.log(`The array contains the value`);
} 

else {
    console.log(`The array does not contain the value`);
}
